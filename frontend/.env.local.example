# URL of the running UniHAP FastAPI backend (uv run unihap api)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public URL this frontend is served from, used for SEO metadata/sitemap/robots
NEXT_PUBLIC_SITE_URL=http://localhost:3000
